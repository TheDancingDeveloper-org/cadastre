# The only executor carrying `gpu` is offline. An availability risk, not a
# guaranteed failure — eligibility is a fact about the catalog, availability is
# a fact about a moment.
name: Build
"on": [push]
x-cadastre:
  system: ci-selfhosted
  repo: notes-api-repo
jobs:
  build:
    runs-on: [self-hosted, linux, gpu]
    steps:
      - run: echo build
